1. Set up virtual environment (use python3 if this doesn't work):
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate

2. Install requirements (use pip3 if this doesn't work):
pip install --no-cache-dir -r requirements.txt

3. Run the entry point using (use python3 if that doesn't work):
python main.py